# 프로젝트 지침

## 파일 저장 규칙

- 시각화 결과물(`.png`, `.jpg`, `.svg` 등 이미지 파일)은 반드시 `image/` 디렉토리에 저장한다.
- 데이터 파일(`.csv`, `.json`, `.parquet` 등)은 `data/` 디렉토리에 저장한다.
- 분석 스크립트(`.py`)는 용도에 따라 `scripts/` 하위 디렉토리에 저장한다.

## 프로젝트 구조

```
data_analytics_final/
├── data/                # 데이터 파일
│   ├── raw/             # 외부 수신 원본 데이터 — git 제외(.gitignore)
│   │   └── migration/   # KT 생활이동 원본 (1441개 파일)
│   ├── main_data/       # 핵심 분析 데이터 (트렌드매출_마스터.csv 등)
│   └── archive/         # 구버전 중간 산출물
├── image/               # 시각화 결과 이미지
├── docs/                # 분석 문서, 데이터 명세
├── report/              # 최종 보고서 파일
├── modeling/            # ML 모델링 스크립트 및 결과
├── scripts/             # 분석 스크립트
│   ├── analysis/        # analysis_*.py — 탐색·시각화 분석
│   ├── build/           # build_*.py — 데이터 가공·보고서 생성
│   ├── crawlers/        # 네이버·블로그 크롤러
│   └── utils/           # 인코딩 변환, 유틸리티
├── .claude/             # Claude Code 설정
├── main.py              # 진입점
├── ANALYSIS_LOG.md      # 분석 흐름 기록
└── readme.md
```

## 핵심 분석 기준 데이터 (우선 사용)

### `data/트렌드매출_마스터.csv` — 트렌드 대표 매출 마스터 파일
**매출 기반 분석은 반드시 이 파일을 1차 데이터로 사용한다.**

- 단위: 행정동 × 연도 (423개 동 × 5년 = 2,114행), utf-8-sig

---

### 핵심 지표 정의

#### 트렌드_대표_매출
```
= 금요일_매출_금액 × 야간비중 + 토요일_매출_금액 + 일요일_매출_금액

야간비중 = (시간대_17~21_매출_금액 + 시간대_21~24_매출_금액) / 전체 시간대 매출 합계
```
- **의미**: "금요일 저녁 퇴근 후 ~ 주말" 구간의 소비 추정액. 트렌드 상권의 핵심 활동 시간대
- **주의**: 원본 데이터에 요일×시간 교차값이 없어 금요일 저녁은 야간비중으로 추정. 금요일 전체에 야간비율을 곱한 근사값

---

#### 트렌드매출_비중
```
= 트렌드_대표_매출 / 총_매출
```
- **의미**: 전체 매출 중 "금요 저녁~주말" 소비가 차지하는 비율
- **해석**: 높을수록 외부 방문객 중심의 주말형 소비 상권. 낮을수록 평일 생활 소비(오피스·거주민) 기반
- **참고 수준**: 서울 전체 평균 약 25~28%

---

#### 업종점수
```
= (F&B카페 + F&B식사 + 주류/유흥 + 라이프스타일) 매출 합계 / 총_매출
```
포함 업종:
- F&B(카페/디저트): 커피-음료, 제과점
- F&B(식사): 한식/일식/양식/중식/분식/패스트푸드/치킨전문점
- 주류/유흥: 호프-간이주점, 노래방
- 라이프스타일: 일반의류, 편의점, 애완동물, 화초, 인테리어

- **의미**: 외부 방문 소비 목적의 업종이 전체 매출에서 차지하는 비율
- **해석**: 높을수록 나들이·여가·소비 목적 방문이 많은 상권. 의료·교육·금융 등 생활 필수 업종은 제외
- **참고 수준**: 트렌드 상권 기준선 약 45~50% 이상

---

#### 금요효과
```
= 금요일_매출_금액 / 목요일_매출_금액
```
- **의미**: 금요일이 목요일 대비 매출이 얼마나 높은가 (주말 유동 시작 신호)
- **해석**:
  - 1.0 이상: 금요일부터 외부 방문객 유입 시작 → 트렌드 상권 특성
  - 1.0 미만: 금요일이 오히려 낮음 → 오피스·거주형 상권 특성
- **왜 목요일 기준인가**: 월~목은 평일 패턴이 비슷하므로 목요일을 평일 기준값으로 사용. 금요일이 목요일보다 높으면 "주말 효과가 금요일부터 선행"한다고 해석
- **참고 수준**: 트렌드 상권 평균 약 1.15~1.35

---

#### 주말비중
```
= (토요일_매출_금액 + 일요일_매출_금액) / 7일 매출 합계
```
- **의미**: 일주일 매출 중 토·일이 차지하는 비율
- **해석**: 높을수록 주말에 외부인이 집중적으로 방문하는 상권
- **참고 수준**:
  - 균등 기준: 2/7 = 28.6% (요일 관계없이 동일하다면)
  - 서울 전체 평균: 약 22~24% (주중이 더 많음)
  - 트렌드 상권: 30% 이상

---

#### 2030비중
```
= (연령대_20_매출_금액 + 연령대_30_매출_금액) / 총_매출
```
- **의미**: 전체 매출 중 20·30대가 소비한 비율
- **사용 원칙**: **트렌드 상권 정의(복합점수)에 포함하지 않는다.** 정의에 넣으면 "2030이 많은 곳을 트렌드 상권으로 정의했더니 2030이 많더라"는 순환논리가 된다. 반드시 **사후 검증** 또는 **종속변수**로만 사용한다.
- **해석**: 트렌드 상권 정의와 독립적으로 계산된 2030 집중도가 높을수록, 우리 정의가 실질을 잘 반영하고 있다는 검증이 됨

---

#### 복합점수 / 복합점수_분위
```
복합점수 = zscore(업종점수) + zscore(시간점수)
시간점수 = zscore(금요효과) + zscore(주말비중) + zscore(야간비중)

복합점수_분위: 해당 연도의 423개 행정동 내에서 상대적 위치 (0=최하위, 1=최상위)
```
- **의미**: 2030 데이터 없이 순수하게 업종 구성과 시간 패턴만으로 산출한 트렌드 상권 지수
- **주의**: 연도 내 표준화이므로 연도 간 절댓값 비교는 불가. 분위(rank) 비교는 가능

---

#### 상권티어 (6단계)

| 티어 | 조건 | 의미 |
|------|------|------|
| ① 레전드 | 분위 ≥ 0.90 + 추세 R² ≥ 0.50 | 꾸준히 최상위 유지 |
| ② 라이징스타 | 분위 ≥ 0.70 + 모멘텀 상위 20% | 상위권에서 계속 성장 |
| ③ 급성장 신흥 | 분위 0.50~0.90 + 모멘텀 상위 10% | 중간에서 빠르게 부상 |
| ④ 잠재 후보 | 분위 0.40~0.70 + 양의 모멘텀 | 주목할 예비 트렌드 상권 |
| ⑤ 성숙·정체 | 분위 ≥ 0.70 + ①②③⑥ 조건 미충족 | 상위권 전부 (모멘텀 무관, 사각지대 없음) |
| ⑥ 쇠퇴 주의 | 2020 상위 30% → 2024 하위 50% | 뚜렷한 하락 추세 |

모멘텀 = 복합점수_분위(2024) - 복합점수_분위(2020)

---

**원본 데이터가 필요한 경우만** 아래 파일을 사용한다:
- `data/서울시_상권분석서비스(추정매출-행정동)_2020_2024.csv` — 행정동별 추정매출 원본 (2020~2024, utf-8-sig)
- `data/서울시 상권분석서비스(추정매출-서울시).csv` — 서울시 전체 추정매출 (utf-8-sig)

## 보조 데이터 파일

- `data/네이버트렌드_트렌드상권_20260517.csv` — 트렌드 상권 15개 네이버 검색 트렌드 (2020~2024, utf-8-sig)
- `data/네이버트렌드_소비키워드_20260517.csv` — 기존 25개 지역 네이버 트렌드 (2020~2024, utf-8-sig)
- `data/블로그트렌드_상권.csv` — 블로그 언급량 (utf-8-sig)

## 분석 기준: 주말 필터 원칙

**모든 유입(이동) 및 매출 분석은 주말 데이터만 기준으로 한다.**

### 이동(유입) 데이터
- **사용 파일**: `data/main_data/분기별_주말유입인구.csv`
- 이 파일은 이미 **금요일 저녁 + 토요일 + 일요일** 이동인구만 포함되어 있으므로 별도 필터 없이 사용한다.
- 세대별 유입 비중 계산 시 이 파일의 연령대별 이동인구를 사용한다.

### 매출 데이터
- **주말 매출** = `토요일_매출_금액 + 일요일_매출_금액` (`분기별_원본피처.csv` 기준)
- 또는 `트렌드매출_마스터.csv`의 `트렌드_대표_매출` 컬럼 사용 (금요 저녁 포함 추정값)
- 전체 매출(`총_매출금액`)을 사용하는 것은 **주말 기준이 아님** — 매출 추세 분석 시 반드시 주말 필터 적용

### 데이터 한계 (주의)
- `분기별_원본피처.csv`에는 **연령대 × 요일 교차 데이터가 없다.**
- 따라서 "주말에 20대가 얼마 썼는지"는 계산 불가.
- 세대별 소비 비중은 전체 주 기준(`연령대_20_매출_금액 / 총_매출`), 세대별 유입 비중은 주말 이동인구 기준으로 구분해서 사용한다.
- 차트나 보고서에서 두 기준이 섞이지 않도록 명확히 표기한다.

---

## 코드 작성 규칙

- 한국어 폰트: `plt.rcParams['font.family'] = 'Malgun Gothic'`
- 마이너스 기호: `plt.rcParams['axes.unicode_minus'] = False`
- 이미지 저장 경로 예시: `image/consume_분석.png`

## ANALYSIS_LOG.md 갱신 규칙

`ANALYSIS_LOG.md`는 분석 흐름을 기록하는 living document다. 아래 상황에서 **반드시** 업데이트한다:

1. **새 분석 스크립트를 작성·실행한 후** — "현재 분석 스크립트 목록" 테이블에 추가
2. **새 데이터 파일을 생성한 후** — "현재 데이터 파일 목록" 테이블에 추가
3. **주요 발견사항이 나온 후** — 해당 Phase 섹션 또는 새 Phase 섹션으로 기록
4. **분석 방향이 바뀐 후** — 이전 접근의 한계와 새 방향을 함께 기록

갱신 형식: Phase N 섹션을 추가하고, 파일 목록 테이블을 업데이트하며, 완료된 항목은 `미결 분석 아이디어`에서 체크한다.
